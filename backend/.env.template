# Backend Environment Variables
# Copy this file to backend/.env and fill in the values

# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/bookmark_db

# OpenAI Configuration
OPENAI_API_KEY=

# Security
SECRET_KEY=

# Environment
ENVIRONMENT=development

# Logging
LOG_LEVEL=INFO

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:3002

# Authentication - Email Whitelisting
# Leave empty to allow all users, or add comma-separated list of whitelisted emails
# Example: WHITELISTED_EMAILS=user1@gmail.com,user2@gmail.com
WHITELISTED_EMAILS=""

# AI Model Configuration (optional - these have defaults)
# EMBEDDING_MODEL=text-embedding-3-small
# EMBEDDING_DIMENSION=1536
# TAG_GENERATION_MODEL=gpt-4o-mini
# CATEGORY_GENERATION_MODEL=gpt-4o-mini
# QUERY_PARSING_MODEL=gpt-4o-mini
# CONTENT_ANALYSIS_MODEL=gpt-4o-mini

# Search Mode: "semantic" (AI embeddings) or "fulltext" (PostgreSQL FTS)
SEARCH_MODE=semantic

# Explore Feed Configuration (optional - these have defaults)
# FEED_REFRESH_INTERVAL_HOURS=24
# FEED_ARTICLES_PER_TOPIC=10
# FEED_MAX_ARTICLES_PER_USER=100
# FEED_ARTICLE_MAX_AGE_DAYS=7